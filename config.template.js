// This shows where to put your claude API key
// Create a new config.js file with the code below in it: 

var config = {
    CLAUDE_API_KEY: 'YOUR_API_KEY_HERE'
  };
  
  export default config;